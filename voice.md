# Voice Profile

Stub — Phase 1 will rewrite this from `~/.claude/voice-profile.md` into the hybrid format documented in the spec (Tone & Dimensions / Lexicon / Anti-patterns / Sample Passages).

Until then, `/jm-voice` continues reading `~/.claude/voice-profile.md` (no behavior change).
