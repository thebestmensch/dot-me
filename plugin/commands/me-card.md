---
effort: low
---

`/me-card`: render the user's saved `~/.me/` context as an ASCII-art
infographic card.

Invoke the `me-card` skill from this plugin. Pass any `$ARGUMENTS`
through (e.g. `--no-color`). Do not improvise; the skill owns the layout.
